version: 0.1

phases:
  pre_build:
    commands:
      - npm install
  build:
     commands:
       - npm run build
  post_build:
     commands:
       - aws s3 cp —recursive ./build s3://www.purplerhapsody.net/
       - aws cloudfront create-invalidation —distribution-id E1NHUG2DY9M4U4 —path /\*